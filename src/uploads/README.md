# Uploads

The purpose of this directory is to emulate a directory where users can upload files on a vulnerable web server. Some attacks, such as registering an evil Service Worker via XSS, requre files to be hosted on the same origin.
